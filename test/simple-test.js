//
//
//

//
var chai = require('chai');
var should = chai.should();
var hcluster = require('..');

//
describe('Hcluster Algorithm with simple case', function() {
  it('builds the correct tree');
});
